# STERBAN0.github.io

Personal engineering portfolio, live at https://sterban0.github.io

Single-file static site: vanilla HTML/CSS/JS, no build step, no dependencies. `index.html` is the whole site.
